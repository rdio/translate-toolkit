--update option
  make recursive methods only run if input is newer than output
make sure extensions, etc are consistent all the way through
  e.g. .dtd -> .dtd.po -> .dtd
handle duplicates in moz2pot and oo2pot
mozilla - automate
  unpackxpi
  convert to .po files
    * make it handle already translated languages well
  convert back
  repackxpi
mozilla - migrate from one version to another

make po files of all existing translations
take over the world

